# qxall Database

> **Note:** Schema design is pending.

This package will contain the database schema, models, and migration scripts for the qxall chess application.

## Migrations

[Alembic](https://alembic.sqlalchemy.org/) will be used for database migrations. The `migrations/` directory is ready for Alembic initialisation (`alembic init migrations`).
